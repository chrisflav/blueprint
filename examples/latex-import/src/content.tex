% The entry file, as `leanblueprint` writes it: nothing but `\input`.
% Comments like this one are stripped before anything else happens, and a
% `\%` is not a comment.
\input{chapters/basics}
\input{chapters/main.tex}
