+++
kind     = "commutes"
boundary = { edges = ["uses/main-theorem/key-prop", "uses/key-prop/compactness", "uses/main-theorem/compactness"] }
title    = "The dependency triangle commutes"
+++
A hyperedge among three `uses` edges: going through [key-prop] and going
directly to [compactness] amount to the same dependency.
